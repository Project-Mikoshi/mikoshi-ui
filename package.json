{
  "name": "root",
  "private": true,
  "scripts": {
    "storybook": "npm run dev && nx start @mikoshi/storybook",
    "dev": "nx run-many --target=dev --all",
    "build": "nx run-many --target=build --all",
    "clean": "nx run-many --target=clean --all",
    "test": "nx affected --target=test --base=develop --head=HEAD",
    "lint": "nx affected --target=lint --base=develop --head=HEAD"
  },
  "devDependencies": {
    "lerna": "^5.4.0",
    "nx": "^14.5.4"
  }
}
