export const DEFAULT_GRID_OPTIONS: Record<string, any> = {
  rowHeight: 50,
  animateRows: true,
  pagination: true,
  enableCellChangeFlash: true,
  defaultColDef: {
    filter: true,
    sortable: true,
    resizable: true,
    flex: 1
  }
}